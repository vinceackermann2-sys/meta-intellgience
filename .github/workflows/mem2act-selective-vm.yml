name: mem2act-selective-vm

on:
  pull_request:
    branches: [main]
    paths:
      - 'researchstrong/selective_slotvm.py'
      - 'researchstrong/strong_banking77.py'
      - '.github/workflows/mem2act-selective-vm.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  selective-vm:
    if: startsWith(github.head_ref, 'research/mem2act-selective-vm')
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install CPU research dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install --disable-pip-version-check --no-cache-dir datasets sentence-transformers scikit-learn
      - name: Run selective residual slot VM
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: python researchstrong/selective_slotvm.py
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mem2act-selective-vm-result
          path: researchstrong/selective_slotvm_result.json
          if-no-files-found: warn
          retention-days: 30
