name: mem2act-gatea-source-selector
on:
  pull_request:
    branches: [main]
    paths:
      - 'researchstrong/gatea_source_selector.py'
      - '.github/workflows/mem2act-gatea-source-selector.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  gatea-source-selector:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install CPU dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install --disable-pip-version-check --no-cache-dir sentence-transformers scikit-learn python-dateutil pycountry webcolors
      - name: Run Gate A source operation selector
        working-directory: researchstrong
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: python gatea_source_selector.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mem2act-gatea-source-selector-result
          path: researchstrong/gatea_source_selector_result.json
          retention-days: 90
