name: semantic-entity-hierarchy-diagnostic
on:
  pull_request:
    branches: [main]
    paths:
      - 'researchstrong/semantic_entity_hierarchy_diagnostic.py'
      - 'researchstrong/semantic_entity_hierarchy_diagnostic_v2.py'
      - '.github/workflows/semantic-world-model.yml'
permissions:
  contents: read
jobs:
  semantic-entity-hierarchy:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install CPU dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install --disable-pip-version-check --no-cache-dir sentence-transformers scikit-learn python-dateutil pycountry webcolors
      - name: Run semantic entity hierarchy diagnostic
        working-directory: researchstrong
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: python semantic_entity_hierarchy_diagnostic_v2.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semantic-entity-hierarchy-result
          path: researchstrong/semantic_entity_hierarchy_diagnostic_result.json
          retention-days: 30
