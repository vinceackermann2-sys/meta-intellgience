name: semantic-entity-scope-oracle
on:
  pull_request:
    branches: [main]
    paths:
      - 'researchstrong/semantic_entity_scope_oracle.py'
      - '.github/workflows/semantic-world-model.yml'
permissions:
  contents: read
jobs:
  entity-scope-oracle:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install CPU dependencies
        run: |
          python -m pip install --disable-pip-version-check --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch
          python -m pip install --disable-pip-version-check --no-cache-dir sentence-transformers scikit-learn python-dateutil pycountry webcolors
      - name: Run semantic entity-record scope oracle
        working-directory: researchstrong
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: python semantic_entity_scope_oracle.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semantic-entity-scope-oracle-result
          path: researchstrong/semantic_entity_scope_oracle_result.json
          retention-days: 30
