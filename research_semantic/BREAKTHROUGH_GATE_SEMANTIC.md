# Semantic World-Model Gate

This branch is development-only until explicitly promoted.

1. QA001-100 may be used for development and 5-fold CV.
2. QA101-400 gold must remain unread on this branch.
3. First sub-gate: value-blind semantic-role bridge must materially improve held-out explicit role binding over raw MiniLM cosine and must not reduce candidate coverage.
4. Before spending any untouched validation labels, the complete memory-to-action development system must exceed 60 parameter F1 with headroom on inferred cases; a role-alignment win alone is not a breakthrough.
5. A breakthrough claim additionally requires untouched validation, sealed final evaluation, and independent transfer to a second longitudinal personalized-tool benchmark.
